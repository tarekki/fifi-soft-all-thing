/**
 * Design System - Main Export
 * نظام التصميم - التصدير الرئيسي
 * 
 * Central export point for all design tokens
 * نقطة التصدير المركزية لجميع توكينات التصميم
 * 
 * Usage:
 * import { colors, typography, spacing, shadows } from '@/lib/design'
 * 
 * الاستخدام:
 * import { colors, typography, spacing, shadows } from '@/lib/design'
 */

export {
  colors,
  typography,
  spacing,
  shadows,
  borderRadius,
  borderWidth,
  transitions,
  zIndex,
  breakpoints,
  productCard,
  buttons,
  badges,
  navigation,
  designTokens,
  type DesignTokens,
  type ColorPalette,
  type TypographyScale,
  type SpacingScale,
} from './tokens'

